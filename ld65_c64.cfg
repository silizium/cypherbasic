MEMORY { 
     RAM:    start = $0801, size = $9800, file = %O; 
} 
SEGMENTS {
	 STARTUP:  load = RAM,    type = rw;
     CODE:     load = RAM,    type = rw; 
     DATA:     load = RAM,    type = rw; 
     BSS:      load = RAM,    type = rw; 
} 
